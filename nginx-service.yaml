apiVersion: v1
kind: Service
metadata:
  name: alfatah-nginx-service
  namespace: alfatah-eks-activity
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80